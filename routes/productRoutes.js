const express = require('express');
const router = express.Router();

// Minimal placeholder router to satisfy require() in app.js.
// The app currently mounts its own product routes; extend here if you later want centralized route files.

module.exports = router;
