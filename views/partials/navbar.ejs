<nav class="navbar navbar-expand-lg navbar-light" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
  <div class="container">
    <a class="navbar-brand" href="/" style="color: white; font-weight: 700; font-size: 24px;">
      <i class="fas fa-shopping-cart"></i> Supermarket
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarItems" style="border-color: rgba(255,255,255,0.3);">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarItems">
      <ul class="navbar-nav ms-auto">
        <% if (user) { %>
          <% if (user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/admin" style="color: white; font-weight: 500;"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
          <% } %>
          <li class="nav-item"><a class="nav-link" href="/products" style="color: white; font-weight: 500;"><i class="fas fa-shopping-bag"></i> Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/cart" style="color: white; font-weight: 500;"><i class="fas fa-shopping-cart"></i> Cart</a></li>
          <% if (user.role !== 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/orders" style="color: white; font-weight: 500;"><i class="fas fa-history"></i> My Orders</a></li>
          <% } %>
          <% if (user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/products/add" style="color: white; font-weight: 500;"><i class="fas fa-plus-circle"></i> Add Product</a></li>
            <li class="nav-item"><a class="nav-link" href="/users" style="color: white; font-weight: 500;"><i class="fas fa-users"></i> Users</a></li>
          <% } %>
          <li class="nav-item"><a class="nav-link" href="/users/logout" style="color: white; font-weight: 500;"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/users/login" style="color: white; font-weight: 500;"><i class="fas fa-sign-in-alt"></i> Login</a></li>
          <li class="nav-item"><a class="nav-link" href="/users/register" style="color: white; font-weight: 500;"><i class="fas fa-user-plus"></i> Register</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
